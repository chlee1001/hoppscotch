query GetUserGroups($limit: Int, $offset: Int, $search: String) {
  userGroups(limit: $limit, offset: $offset, search: $search) {
    id
    name
    description
    role
    createdAt
    memberCount
    teamCount
  }
}
