mutation CreateUserGroup($name: String!, $description: String, $role: UserGroupTeamAccessRole!) {
  createUserGroup(name: $name, description: $description, role: $role) {
    id
    name
    description
    role
    createdAt
  }
}
