mutation UpdateUserGroup(
  $groupId: ID!
  $name: String
  $description: String
  $role: UserGroupTeamAccessRole
) {
  updateUserGroup(
    groupId: $groupId
    name: $name
    description: $description
    role: $role
  ) {
    id
    name
    description
    role
  }
}
