query MyUserGroups {
  myUserGroups {
    id
    name
    description
    role
    isAdmin
    members {
      id
      userUid
      groupId
      isAdmin
      addedAt
      user {
        uid
        displayName
        email
        photoURL
      }
    }
  }
}
